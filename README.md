## My static github page

How to: https://help.github.com/articles/using-jekyll-with-pages/

For local devl:

`bundle install`

generate `./_site` folder
`$ jekyll build`

start server accessible on localhost:4000
`jekyll serve`
